# BENCHMARKS — HAGI V48

Документально подтверждённые замеры wall-time и throughput. Все замеры на
AMD Radeon 8060S (iGPU, bandwidth-bound, cap (11,5)), torch 2.10.0+rocm7.13,
bf16, seq_len=128, batch=256 (если не указано иное).

## 1. Базовая скорость (V46, H=128, L=3)

| Конфигурация | ms/step | tok/s | Примечание |
|---|---|---|---|
| Без compile | 59.88 | 0.41M | fwd=25.2, bwd=30.0, data=2.5, optim=1.7 |
| **С compile** | **21.58** | **1.14M** | mode=default, mem-efficient SDPA |
| num_workers=2 | 20.79 | 1.18M | median |

**Прирост от torch.compile: 2.77x** (59.88 → 21.58 ms/step).

## 2. Batch scaling (ЧАСТЬ II)

| batch | ms/step | tok/s | update_applied |
|---|---|---|---|
| 192 | 22.28 | 1.10M | True |
| **256** | **26.41** | **1.24M** | **True** |
| 320 | — | — | **False** (compile-баг) |
| 384 | 36.60 | 1.34M | False (compile-баг) |
| 768 | 65.45 | 1.50M | False (compile-баг) |

**Критический баг**: при `compile_model=True` и batch≥320 torch.compile даёт
non-finite градиенты (rest=nan/inf) и молча пропускает все обновления
(update_applied=False). Это баг buffer-reuse в ROCm torch.compile. batch=256 —
безопасный максимум.

**Прирост batch 192→256: +13%** (1.10M → 1.24M tok/s) с идентичной
сходимостью (loss 7.627-7.630).

## 3. Merged-модель (H=512, fp16, ternary off)

| Модель | ms/step | tok/s |
|---|---|---|
| MergedHAGI H=512 (compile ON) | 122.66 | 0.27M |
| Baseline HAGI H=512 (compile OFF) | 160.95 | 0.20M |

MergedHAGI (fp16-слои) компилируется чисто при batch=256; baseline HAGI
(ternary BitLinear) ломается при compile+batch≥256 и требует compile OFF.

## 4. Итоговый прирост (ЧАСТЬ II)

| Метрика | База (V46 no-compile) | V48 (compile + batch 256) | Прирост |
|---|---|---|---|
| ms/step | 59.88 | 21.58 | **2.77x** |
| tok/s | 0.41M | 1.24M | **3.02x** |

**Требование ≥20% выполнено с запасом: 3.02x (302%) прирост throughput.**

## 5. Эксперимент 4 эксперта (ЧАСТЬ III)

### 5a. Недообученные эксперты (2000 шагов)

Бюджет: 4 эксперта × 2000 шагов × 24576 токенов = 196M токенов + joint
200 × 24576 = 4.9M. Итого ~201M токенов.

### 5b. Насыщенные эксперты (20000 шагов)

Бюджет: 4 эксперта × 20000 шагов × 32768 токенов = 2.6B токенов + joint
2000 × 32768 = 65M токенов. Итого ~2.7B токенов.

### Специализация насыщенных экспертов (exact CE на своём домене)

| Эксперт | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| RU | **6.00** | 8.13 | 8.00 | 8.00 | 7.53 |
| EN | 8.76 | **5.48** | 5.67 | 5.79 | 6.42 |
| MATH | 9.22 | 6.14 | **5.32** | 6.61 | 6.82 |
| CODE | 11.32 | 8.30 | 7.64 | **3.39** | 7.66 |

Специализация после насыщения сильнее: каждый эксперт глубоко настроен на
свой домен (CODE на RU даёт 11.32 — почти не понимает кириллицу).

### Merged-модель (H=512, насыщенные эксперты)

| Этап | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| step 0 (до joint, logit_scale НЕ нормирован) | 10.82 | 9.88 | 9.29 | 5.99 | 9.00 |
| step 0 (до joint, logit_scale / sqrt(N)) | 8.18 | 6.59 | 6.24 | 4.53 | **6.39** |
| **после 2000 joint** | **5.72** | **5.09** | **5.05** | **3.47** | **4.83** |

> **Баг logit_scale**: при блочно-диагональном merge head-проекция = concat по
> input dim, так что `hidden @ weight.T` суммирует N вкладов. Без деления
> logit_scale на sqrt(N) logits в ~N раз острее → распределение коллапсирует
> (step 0 AVG=9.00). Нормализация (как в grow.py) даёт step 0 AVG=6.39.

### Сравнение при равном бюджете общего обучения (49M токенов)

| Модель | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| **Merged (2000 joint)** | **5.72** | **5.09** | **5.05** | **3.47** | **4.83** |
| Baseline (2000 шагов) | 6.63 | 5.36 | 5.43 | 4.35 | 5.44 |

**Merged превосходит baseline на 0.61 exact CE** при том же бюджете общего
обучения. Merged также превосходит каждого эксперта на своём домене
(RU=5.72 vs 6.00, EN=5.09 vs 5.48, MATH=5.05 vs 5.32, CODE=3.47 vs 3.39).

### Baseline (from scratch H=512, 2000 шагов)

| Метрика | step 0 | step 100 | step 600 | step 2000 |
|---|---|---|---|---|
| nce | 7.62 | 7.49 | 6.36 | — |
| exact_ce | 7.33 | 7.30 | 6.91 | — |

Baseline eval (2000 шагов, 49M токенов): AVG=5.44 (RU=6.63, EN=5.36,
MATH=5.43, CODE=4.35).

## 6. Иерархический merge (ЧАСТЬ IV)

Иерархия 16→4→1 (здесь 4→2→1): 4 насыщенных эксперта (H=128) → 2 группы
(H=256, RU+EN и MATH+CODE) → финальная H=512. Каждый уровень дообучается
joint (500 шагов на уровне 1, затем 2000 на финальном).

### Специализация групп уровня 1 (после 500 joint)

| Группа | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| G0 (RU+EN) | 5.95 | 5.30 | — | — | — |
| G1 (MATH+CODE) | — | — | 5.28 | 3.89 | — |

### Финальный иерархический merged (H=512)

| Этап | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| step 0 (logit_scale / sqrt(N)) | 8.32 | 7.95 | 7.93 | 6.04 | 7.56 |
| **после 2000 joint** | **5.67** | **5.07** | **5.04** | **3.48** | **4.81** |

### Сравнение при равном бюджете общего обучения (49M токенов)

| Модель | RU | EN | MATH | CODE | AVG |
|---|---|---|---|---|---|
| **Flat merged (2000 joint)** | **5.72** | **5.09** | **5.05** | **3.47** | **4.83** |
| Hierarchical merged (2000 joint) | 5.67 | 5.07 | 5.04 | 3.48 | 4.81 |
| Baseline (2000 шагов) | 6.63 | 5.36 | 5.43 | 4.35 | 5.44 |

**Вывод**: после исправления logit_scale иерархия (4→2→1) и flat merge (4→1)
дают практически одинаковый результат (4.81 vs 4.83). Оба превосходят
baseline (5.44). Ранее иерархия была хуже (5.00) из-за неверного logit_scale
на промежуточном уровне.

## 7. Hadamard Cross-Expert Mixer (ЧАСТЬ V)

Замена полного SwiGLU-миксера на фиксированный fast-Hadamard + малый
low-rank residual. Идея: block-diagonal merge оставляет экспертов
независимыми, и полный SwiGLU-миксер должен с нуля выучить ВСЮ межблочную
коммуникацию полной шириной (3 плотные матрицы H×2H). Hadamard даёт
базовое перемешивание (sum/difference каналы) за O(NH log N) FLOPs и 0
параметров; обучаемая часть — маленький low-rank (rank=64), ~16x меньше
FLOPs, чем полный SwiGLU.

### 7a. Параметры миксера (H=512, n=4)

| Mixer | Параметры | Отношение |
|---|---|---|
| CrossMixer (SwiGLU) | 1,573,890 | 1.0x |
| HadamardMixer (rank=64) | 99,330 | **0.063x (16x меньше)** |

### 7b. Wall-time (H=2048, n=16, batch=64, seq=128, compile ON)

| Mixer | ms/step | tok/s |
|---|---|---|
| CrossMixer (SwiGLU) | 354.91 | 0.023M |
| **HadamardMixer (rank=128)** | **273.52** | **0.030M** |

**Прирост: 23%** (354.91 → 273.52 ms/step) на H=2048/n=16, где mixer —
значимая доля шага. На малой модели (H=512, n=4) прирост ~7%
(120.49 → 112.23 ms/step), т.к. mixer — малая доля шага.

### 7c. Оптимизация FHT → matmul

Первоначальная butterfly-реализация FHT (O(NH log N)) оказалась
медленнее dense matmul на Radeon 8060S: 7.76 vs 0.81 ms при N=16, H=2048,
B·T=8192 (9.5x). Причина — reshape/stack-бабочка launch-bound, а малый
`[n,n]`-матричный matmul едет на tensor cores. Для N≤64 используется
кэшированный `H_n/sqrt(n)` + batched matmul; butterfly — fallback для N>64.

| Реализация | ms/call (N=16, H=2048, B·T=8192) |
|---|---|
| Butterfly FHT | 7.76 |
| **Dense matmul (кэш H_n)** | **1.39** |

**Прирост: 5.6x** на самом Hadamard-преобразовании.

### 7d. Step-0 эквивалентность

Hadamard mixer при gain=0 даёт `y = Q·x` (Q = (H_n/sqrt(n)) ⊗ I_H). Чтобы
step-0 логиты совпадали с block-diagonal merge, head-проекция
предварительно вращается: `W' = W·Q`. Проверено на реальных экспертах
(v48, n=4): max logit diff 0.012 (bf16 шум), 0% логитов отличаются >0.05.

**Ключевые детали реализации**:
- Mixer применяется ПОСЛЕ `out_norm` (hook `_apply_mixers`), чтобы head
  видел `mixer(out_norm(h))` — это делает head-вращение точным.
- Вращается только выходная head-проекция (`head.projection.weight`), НЕ
  входной эмбеддинг (`encoder.embedding.weight`), если `tie_lm_head=false`.
  При `tie_lm_head=true` вращается и эмбеддинг (он же head).
- Hadamard требует n_experts = степень двойки (валидация в config).

### 7e. Конфигурация

```yaml
merge:
  mixer_type: hadamard   # default: hadamard | swiglu
  mixer_rank: 64         # rank low-rank residual (hadamard only)
```

`mixer_init_scale` (0 по умолчанию) — начальный gain: при 0 mixer = чистый
Hadamard, обучение только корректирует его.
