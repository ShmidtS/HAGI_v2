/target/
node_modules/
build/
packages/*/dist/
*.tsbuildinfo
.DS_Store
/tmp/
*.db
package-lock.json
gh-cli-cache/
tasks/
AGENTS.md
.claude/worktrees/
.claude-worktrees/
.vibetree/
.cw/
/.cocoindex_code/
/.kilo/
.omc/
meta.json
.env
/data/
/.opencode/

# cargo install artifacts
.crates.toml
.crates2.json
.context-mode/

# untracked / wip artifacts
formalization/.lake/
formalization/.lake-build.log
docs/superpowers/

# Python
__pycache__
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
dist/
*.pyc

# checkpoints and model weights
checkpoints/
checkpoints_dsv4/
*.pt
*.pth
*.ckpt
*.log
*.safetensors
consumed.json
torch-compile.bat
profile_*
profiler_logs/

# refit runtime done-logs (append-only state)
refit_*.txt
dsv4_shared_only/
research_refs/
seq_full.lock
seq_full_pass.log
seq_full_done.txt
collect_full.log
refit_full.log
checkpoints_dsv4/seq_full/

# v2 pass runtime artifacts (2026-09-03)
*.lock
*.out
refit_v2_*.log
collect_v2.log
seq_full_pass_v2.log
seq_full_done_v2.txt
canary_done.txt
checkpoints_dsv4/seq_v2/
checkpoints_dsv4/pod_all_tokens/

# one-shot diagnostics (results live in logs; kept on disk, not in git)
canary_345.sh
canary_345_phase2.sh
l5_resume.sh
scripts/repro_l5_crash.py
scripts/scan_acts_finite.py
scripts/scan_acts_order.py

dsv4_release/
dsv4_bank/
dsv4_latent_gate.pt*
_v41/report.txt
eval/
glm5_gguf/
glm5_pod/
glm5_kvp/
models/
reports/
.pi/
*.diff

# vendored llama.cpp forks — each has its own .git and its own remotes;
# tracked upstream, never in this repo (2.4 GB of build trees + .git)
llama-glm5/
llama-ds4/

# agent working state (not project source)
.omo/
evolve_state/
self_talk_logs/

# archived spent code IS kept in history (277 KB of source + MANIFEST); only the
# regenerable GGUF metadata dumps stay out
_archive/**/*.json

# one-shot audit/cleanup harnesses, at the repo root only (the archived copies
# under _archive/ are the record of how this cleanup was derived — those stay
# tracked). Anchored with / so they do not swallow the archive.
/_audit_*.py
/_audit_*.json
/_cleanup.py
